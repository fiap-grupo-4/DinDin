import { Meta, Title, Subtitle } from "@storybook/addon-docs/blocks";

import { IconCatalog } from "./IconCatalog";

<Meta title="Foundation/Icons" />

<Title>Icons</Title>

<Subtitle>
  SVG icons are exported from <code>src/components/ui/Icon/partials/</code> and
  rendered through the <code>Icon</code> component (
  <code>name</code> prop). Names match the partial file exports (PascalCase).
</Subtitle>

<IconCatalog />
